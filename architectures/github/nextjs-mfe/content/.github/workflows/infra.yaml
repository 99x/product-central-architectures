name: Setting Up Infrastructure for Micro-Frontend Application using Terraform in AWS

on:
  workflow_dispatch:
    inputs:
      parameters:
        description: "Parameters for the workflow"
        required: true

jobs:
  provision:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~> 1.5.7"

      - name: Provisioning Infrastructure using Terraform
        run: |
          set -eo pipefail
          cd infra
          ./apply.sh '${{ github.event.inputs.parameters }}'
